@model RestauranteMVC.Models.Mesa

@{
    ViewData["Title"] = "Detalles de la Mesa";
}

<div class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-info text-white">
            <h2><i class="bi bi-eye"></i> Detalles de la Mesa</h2>
        </div>
        <div class="card-body">

            <dl class="row">
                <dt class="col-sm-4">ID:</dt>
                <dd class="col-sm-8">@Model.IdMesa</dd>

                <dt class="col-sm-4">Número:</dt>
                <dd class="col-sm-8">@Model.Numero</dd>

                <dt class="col-sm-4">Capacidad:</dt>
                <dd class="col-sm-8">@Model.Capacidad</dd>

                <dt class="col-sm-4">Estado:</dt>
                <dd class="col-sm-8">@Model.Estado</dd>
            </dl>

            <div class="text-end">
                <a asp-action="Edit" asp-route-id="@Model.IdMesa" class="btn btn-warning">Editar</a>
                <a asp-action="Index" class="btn btn-secondary">Volver</a>
            </div>

        </div>
    </div>
</div>
