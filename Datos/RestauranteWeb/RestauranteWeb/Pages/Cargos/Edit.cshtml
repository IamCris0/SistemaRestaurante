@page "{id:int}"
@model RestauranteWeb.Pages.Cargos.EditModel
@{
    ViewData["Title"] = "Editar Cargo";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-warning">
                    <h4><i class="bi bi-pencil"></i> Editar Cargo</h4>
                </div>
                <div class="card-body">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger">
                            @TempData["Error"]
                        </div>
                    }

                    <form method="post">
                        <div class="mb-3">
                            <label asp-for="Cargo.Idcargo" class="form-label">ID Cargo:</label>
                            <input asp-for="Cargo.Idcargo" class="form-control" readonly />
                        </div>

                        <div class="mb-3">
                            <label asp-for="Cargo.Nombre" class="form-label">Nombre:</label>
                            <input asp-for="Cargo.Nombre" class="form-control" maxlength="50" required />
                            <span asp-validation-for="Cargo.Nombre" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Cargo.Descripcion" class="form-label">Descripción:</label>
                            <textarea asp-for="Cargo.Descripcion" class="form-control" rows="3" maxlength="150"></textarea>
                            <span asp-validation-for="Cargo.Descripcion" class="text-danger"></span>
                        </div>

                        <div class="mt-4">
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-save"></i> Guardar Cambios
                            </button>
                            <a asp-page="Index" class="btn btn-secondary">
                                <i class="bi bi-x-circle"></i> Cancelar
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
}